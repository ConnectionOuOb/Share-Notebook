# cat /etc/network/interfaces

# network interface settings; autogenerated
# Please do NOT modify this file directly, unless you know what
# you're doing.
#
# If you want to manage parts of the network configuration manually,
# please utilize the 'source' or 'source-directory' directives to do
# so.
# PVE will preserve these directives, but will NOT read its network
# configuration from sourced files, so do not attempt to move any of
# the PVE managed interfaces into external files!

auto lo
iface lo inet loopback

iface eno2 inet manual

iface enx00e04c681c43 inet manual

auto vmbr0
iface vmbr0 inet static
        address {內網IP/CIDR}
        bridge-ports eno2
        bridge-stp off
        bridge-fd 0

iface wlo1 inet manual

auto vmbr1
iface vmbr1 inet static
        address {外網IP/CIDR}
        gateway {外網Gateway}
        bridge-ports enx00e04c681c43
        bridge-stp off
        bridge-fd 0

auto vmbr2
iface vmbr2 inet static
        address {NAT IP/CIDR}
        netmask 255.255.255.0
        bridge-ports none
        bridge-stp off
        bridge-fd 0

post-up echo 1 > /proc/sys/net/ipv4/ip_forward
post-up iptables -t nat -A POSTROUTING -s '{NAT 網段IP/CIDR}' -o vmbr1 -j MASQUERADE
post-down iptables -t nat -D POSTROUTING -s '{NAT 網段IP/CIDR}' -o vmbr1 -j MASQUERADE
post-up   iptables -t raw -I PREROUTING -i fwbr+ -j CT --zone 1
post-down iptables -t raw -D PREROUTING -i fwbr+ -j CT --zone 1